<div>
  <div class="row" value="{{post.id}}" name="post-id">
    <div class="col-auto">
      <h2>{{post.user.name}}</h2>
    </div>
  </div>

  <div class="row mt-4">
    <div class="col-md-6">
      <h3>{{post.title}}</h3>
      <p>{{post.content}}</p>
    </div>
  </div>

  <div>
    <input type="text" id="commentBody">
    <button></button>
  </div>
</div>