<div class="row">
  {{#each posts}}
  <section class="card">
    <a href="dashboard/edit/{{id}}">
      <header>{{title}}</header>
      <p class="card-body">{{content}}</p>
    </a>
  </section>
  {{/each}}
</div>